Welcome the nats streaming/jetstream replicator.
The setup replicates the following topics from the streaming side on {{ .Values.channel }}.

{{ range $name := .Values.topics }}
  - {{ $name }}
{{ end }}
